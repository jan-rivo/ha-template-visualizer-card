{
  "name": "ha-template-editor",
  "version": "0.1.0",
  "description": "Home Assistant Lovelace card that parses a template sensor's Jinja logic and visualizes which sub-conditions are true/false, and why the overall result is what it is.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/**/*.test.ts"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.23.1",
    "typescript": "^5.6.3",
    "tsx": "^4.19.1"
  },
  "dependencies": {
    "lit": "^3.2.0"
  }
}
